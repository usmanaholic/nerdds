# Snack Feature - Architecture Diagram

```
┌─────────────────────────────────────────────────────────────────────────┐
│                          SNACK ARCHITECTURE                              │
└─────────────────────────────────────────────────────────────────────────┘

┌───────────────────────────────────────────────────────────────────────────┐
│                             FRONTEND (React)                               │
├───────────────────────────────────────────────────────────────────────────┤
│                                                                            │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐ │
│  │   Snack      │  │   Create     │  │   Waiting    │  │   Active     │ │
│  │   Home       │→ │   Dialog     │→ │   Screen     │→ │   Session    │ │
│  │             │  │             │  │             │  │             │ │
│  │  - Stats     │  │  - Type      │  │  - Animation │  │  - Chat      │ │
│  │  - Types     │  │  - Tags      │  │  - Cancel    │  │  - Timer     │ │
│  │  - How To    │  │  - Duration  │  │  - Poll      │  │  - Actions   │ │
│  └──────────────┘  └──────────────┘  └──────────────┘  └──────────────┘ │
│           │                │                 │                 │          │
│           └────────────────┴─────────────────┴─────────────────┘          │
│                                       │                                    │
│                                       ▼                                    │
│                    ┌─────────────────────────────────┐                    │
│                    │      React Query Hooks          │                    │
│                    │                                 │                    │
│                    │  - useSnackMatchStatus()        │                    │
│                    │  - useCreateSnackRequest()      │                    │
│                    │  - useSendSnackMessage()        │                    │
│                    │  - useRateSnackSession()        │                    │
│                    └─────────────────────────────────┘                    │
│                              │              │                              │
└──────────────────────────────┼──────────────┼──────────────────────────────┘
                               │              │
                      REST API │              │ WebSocket
                               │              │
┌──────────────────────────────┼──────────────┼──────────────────────────────┐
│                              ▼              ▼                              │
│                           BACKEND (Express + Socket.io)                    │
├───────────────────────────────────────────────────────────────────────────┤
│                                                                            │
│  ┌─────────────────────────────────────────────────────────────────────┐ │
│  │                        REST API Routes                               │ │
│  │  /api/snack/request (POST)      - Create snack request              │ │
│  │  /api/snack/request/:id (DELETE) - Cancel request                   │ │
│  │  /api/snack/match-status (GET)  - Check status                      │ │
│  │  /api/snack/rate (POST)         - Rate session                      │ │
│  │  /api/snack/report (POST)       - Report user                       │ │
│  │  /api/snack/block (POST)        - Block user                        │ │
│  └─────────────────────────────────────────────────────────────────────┘ │
│                                       │                                    │
│                                       ▼                                    │
│  ┌─────────────────────────────────────────────────────────────────────┐ │
│  │                     Matching Service                                 │ │
│  │                                                                      │ │
│  │  attemptMatch(requestId)                                            │ │
│  │    ├─ Find waiting requests (same type)                            │ │
│  │    ├─ Filter blocked/reported users                                │ │
│  │    ├─ Score candidates:                                             │ │
│  │    │   • Tag similarity (60%)                                       │ │
│  │    │   • Duration match (20%)                                       │ │
│  │    │   • Location match (20%)                                       │ │
│  │    ├─ Select best match (score >= 0.3)                             │ │
│  │    └─ Create session                                                │ │
│  │                                                                      │ │
│  │  submitRating(sessionId, rating)                                    │ │
│  │    ├─ Update session with rating                                   │ │
│  │    └─ Calculate & update user snackScore                           │ │
│  │                                                                      │ │
│  │  blockUser(), reportUser(), extendSession()                         │ │
│  └─────────────────────────────────────────────────────────────────────┘ │
│                                       │                                    │
│                                       ▼                                    │
│  ┌─────────────────────────────────────────────────────────────────────┐ │
│  │                     Socket.io Events                                 │ │
│  │                                                                      │ │
│  │  snack:authenticate      - User connects                            │ │
│  │  snack:join-session      - Join chat room                           │ │
│  │  snack:send-message      - Send chat message                        │ │
│  │  snack:typing            - Typing indicator                         │ │
│  │  snack:request-extend    - Request more time                        │ │
│  │  snack:end-session       - End session                              │ │
│  │  snack:matched           - Match found (emit)                       │ │
│  │  snack:new-message       - New message (emit)                       │ │
│  │  snack:session-ended     - Auto-expire (emit)                       │ │
│  └─────────────────────────────────────────────────────────────────────┘ │
│                                       │                                    │
│                                       ▼                                    │
│  ┌─────────────────────────────────────────────────────────────────────┐ │
│  │                     Storage Layer                                    │ │
│  │                                                                      │ │
│  │  createSnackRequest()                                               │ │
│  │  getMyActiveSnackRequest()                                          │ │
│  │  getSnackSession()                                                  │ │
│  │  getMyActiveSnackSession()                                          │ │
│  │  getSnackMessages()                                                 │ │
│  │  createSnackMessage()                                               │ │
│  └─────────────────────────────────────────────────────────────────────┘ │
│                                       │                                    │
└───────────────────────────────────────┼────────────────────────────────────┘
                                        │
                                        ▼
┌───────────────────────────────────────────────────────────────────────────┐
│                          DATABASE (PostgreSQL)                             │
├───────────────────────────────────────────────────────────────────────────┤
│                                                                            │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐ │
│  │    users     │  │snack_requests│  │snack_sessions│  │snack_messages│ │
│  ├──────────────┤  ├──────────────┤  ├──────────────┤  ├──────────────┤ │
│  │ id           │  │ id           │  │ id           │  │ id           │ │
│  │ username     │  │ created_by   │  │ user1_id     │  │ session_id   │ │
│  │ snack_score  │  │ snack_type   │  │ user2_id     │  │ sender_id    │ │
│  │ snack_count  │  │ topic        │  │ snack_type   │  │ content      │ │
│  │ ...          │  │ duration     │  │ duration     │  │ created_at   │ │
│  └──────────────┘  │ tags         │  │ started_at   │  └──────────────┘ │
│                    │ location     │  │ expires_at   │                    │
│  ┌──────────────┐  │ status       │  │ status       │  ┌──────────────┐ │
│  │snack_blocks  │  │ created_at   │  │ rating_user1 │  │snack_reports │ │
│  ├──────────────┤  └──────────────┘  │ rating_user2 │  ├──────────────┤ │
│  │ blocker_id   │                    │ ended_at     │  │ reporter_id  │ │
│  │ blocked_id   │                    └──────────────┘  │ reported_id  │ │
│  │ created_at   │                                      │ session_id   │ │
│  └──────────────┘                                      │ reason       │ │
│                                                         │ description  │ │
│                                                         └──────────────┘ │
└───────────────────────────────────────────────────────────────────────────┘


USER FLOW DIAGRAM
═════════════════

┌─────────┐
│  User   │
│ Opens   │
│ /snack  │
└────┬────┘
     │
     ▼
┌─────────────────┐
│  Home Screen    │     No active request/session
│  - View Stats   │
│  - Select Type  │
└────┬────────────┘
     │ Clicks "Find Match"
     ▼
┌─────────────────┐
│ Create Dialog   │
│ - Type: Study   │
│ - Tags: math    │
│ - Duration: 15  │
└────┬────────────┘
     │ Submit
     ▼
┌─────────────────┐         ┌──────────────────┐
│  POST /request  │────────▶│ Matching Service │
└────┬────────────┘         │ - Find candidate │
     │                      │ - Score matches  │
     │                      │ - Create session │
     │                      └────────┬─────────┘
     │                               │
     ├──── No Match ────────────┐    │
     │                          │    │
     ▼                          │    │ Match Found
┌─────────────────┐             │    │
│ Waiting Screen  │             │    │
│ - Animated      │◀────────────┘    │
│ - Poll every 3s │                  │
│ - Can cancel    │                  │
└─────────────────┘                  │
                                     ▼
                            ┌─────────────────┐
                            │ Socket.io Emit  │
                            │ "snack:matched" │
                            └────┬────────────┘
                                 │
                                 ▼
                        ┌──────────────────┐
                        │  Active Session  │
                        │  - Timer: 15:00  │
                        │  - Chat real-time│
                        │  - Actions       │
                        └────┬─────────────┘
                             │ Timer expires
                             ▼
                        ┌──────────────────┐
                        │  Summary Modal   │
                        │  - Rate 1-5 ⭐   │
                        │  - Add friend    │
                        │  - Report/Block  │
                        └────┬─────────────┘
                             │
                             ▼
                        ┌──────────────────┐
                        │   Back to Home   │
                        │ - Updated stats  │
                        └──────────────────┘


MATCHING ALGORITHM FLOW
════════════════════════

User A creates request
         │
         ▼
  ┌─────────────┐
  │  Queue Add  │
  └──────┬──────┘
         │
         ▼
  ┌──────────────────┐
  │ Find candidates  │
  │  - Same type     │
  │  - Same campus   │
  │  - Waiting       │
  └──────┬───────────┘
         │
         ▼
  ┌──────────────────┐
  │ Filter blocked   │
  │ - User blocks    │
  │ - Reports        │
  └──────┬───────────┘
         │
         ▼
  ┌──────────────────┐
  │ Score candidates │
  │  Tag: 60%       │
  │  Duration: 20%  │
  │  Location: 20%  │
  └──────┬───────────┘
         │
         ▼
  ┌──────────────────┐
  │ Best match?      │
  │ (score >= 0.3)   │
  └──────┬───────────┘
         │
    ┌────┴────┐
    │         │
   YES        NO
    │         │
    ▼         ▼
┌────────┐  ┌──────┐
│Create  │  │ Wait │
│Session │  │ More │
└────────┘  └──────┘


DATA FLOW EXAMPLE
═════════════════

User creates "Study" request with tags: ["math", "homework"]
                          │
                          ▼
                  POST /api/snack/request
                  {
                    snackType: "study",
                    tags: ["math", "homework"],
                    duration: 15
                  }
                          │
                          ▼
              storage.createSnackRequest()
                          │
                          ▼
              INSERT INTO snack_requests
                  (created_by, snack_type, tags, ...)
                          │
                          ▼
              attemptMatch(requestId)
                          │
              finds waiting request with
              tags: ["math", "physics"]
                          │
              calculates tag similarity:
              intersection: ["math"]
              union: ["math", "homework", "physics"]
              score: 1/3 = 0.33 ✓
                          │
                          ▼
              CREATE snack_session
                  (user1_id, user2_id, ...)
                          │
                          ▼
              Socket.io broadcast
              "snack:matched" to both users
                          │
                          ▼
              Frontend updates, shows chat
```

This architecture provides:
- ✅ Separation of concerns
- ✅ Real-time capabilities
- ✅ Scalable matching
- ✅ Type safety
- ✅ Error handling
- ✅ Security

---

**Ready for production! 🚀**
