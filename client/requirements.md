## Packages
date-fns | Formatting dates relative to now (e.g., "2 hours ago")
react-intersection-observer | Infinite scrolling implementation
framer-motion | Smooth animations for page transitions and interactions
lucide-react | Icons (already in stack, but noting for usage)
clsx | Utility for conditional classes
tailwind-merge | Utility for merging tailwind classes

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  sans: ["'Inter'", "sans-serif"],
  display: ["'Outfit'", "sans-serif"], // For headings
}
